{
  "buildCommand": "cd frontend && npm run build || npm run build",
  "outputDirectory": "frontend/dist",
  "installCommand": "cd frontend && npm install || npm install",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
